#ifndef AAS_CORE_AAS_3_0_COMMON_GUARD_
#define AAS_CORE_AAS_3_0_COMMON_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include <functional>
#include <sstream>
#include <string>
#include <utility>

// See: https://stackoverflow.com/questions/2324658/how-to-determine-the-version-of-the-c-standard-used-by-the-compiler
#if ((defined(_MSVC_LANG) && _MSVC_LANG >= 201703L) || __cplusplus >= 201703L)
// Standard library provides std::optional in C++17 and above.
#include <optional>
#else
// We rely on https://github.com/TartanLlama/optional for optional structure.
#include <tl/optional.hpp>
#endif

/**
 * \defgroup common Common functionality used throughout the library
 */
/**@{*/

namespace aas_core {
namespace aas_3_0 {
namespace common {

// Please keep in sync with the preprocessing directives above in the include block. 
#if ((defined(_MSVC_LANG) && _MSVC_LANG >= 201703L) || __cplusplus >= 201703L)
// Standard library provides std::optional in C++17 and above.
using std::optional;
using std::nullopt; 
using std::make_optional;
#else
using tl::optional;
using tl::nullopt;
using tl::make_optional;
#endif

// Standard library provides std::make_unique in C++14 and above.
#if ((defined(_MSVC_LANG) && _MSVC_LANG >= 201402L) || __cplusplus >= 201402L)
using std::make_unique;
#else
// Inspired by:
// https://stackoverflow.com/questions/12547983/is-there-a-way-to-write-make-unique-in-vs2012
template<
  typename T,
  typename A1
>
std::unique_ptr<T> make_unique(
  A1&& a1
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9,
  typename A10
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9,
  A10&& a10
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9),
      std::forward<A10>(a10)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9,
  typename A10,
  typename A11
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9,
  A10&& a10,
  A11&& a11
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9),
      std::forward<A10>(a10),
      std::forward<A11>(a11)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9,
  typename A10,
  typename A11,
  typename A12
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9,
  A10&& a10,
  A11&& a11,
  A12&& a12
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9),
      std::forward<A10>(a10),
      std::forward<A11>(a11),
      std::forward<A12>(a12)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9,
  typename A10,
  typename A11,
  typename A12,
  typename A13
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9,
  A10&& a10,
  A11&& a11,
  A12&& a12,
  A13&& a13
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9),
      std::forward<A10>(a10),
      std::forward<A11>(a11),
      std::forward<A12>(a12),
      std::forward<A13>(a13)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9,
  typename A10,
  typename A11,
  typename A12,
  typename A13,
  typename A14
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9,
  A10&& a10,
  A11&& a11,
  A12&& a12,
  A13&& a13,
  A14&& a14
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9),
      std::forward<A10>(a10),
      std::forward<A11>(a11),
      std::forward<A12>(a12),
      std::forward<A13>(a13),
      std::forward<A14>(a14)
    )
  );
}

template<
  typename T,
  typename A1,
  typename A2,
  typename A3,
  typename A4,
  typename A5,
  typename A6,
  typename A7,
  typename A8,
  typename A9,
  typename A10,
  typename A11,
  typename A12,
  typename A13,
  typename A14,
  typename A15
>
std::unique_ptr<T> make_unique(
  A1&& a1,
  A2&& a2,
  A3&& a3,
  A4&& a4,
  A5&& a5,
  A6&& a6,
  A7&& a7,
  A8&& a8,
  A9&& a9,
  A10&& a10,
  A11&& a11,
  A12&& a12,
  A13&& a13,
  A14&& a14,
  A15&& a15
) {
  return std::unique_ptr<T>(
    new T(
      std::forward<A1>(a1),
      std::forward<A2>(a2),
      std::forward<A3>(a3),
      std::forward<A4>(a4),
      std::forward<A5>(a5),
      std::forward<A6>(a6),
      std::forward<A7>(a7),
      std::forward<A8>(a8),
      std::forward<A9>(a9),
      std::forward<A10>(a10),
      std::forward<A11>(a11),
      std::forward<A12>(a12),
      std::forward<A13>(a13),
      std::forward<A14>(a14),
      std::forward<A15>(a15)
    )
  );
}
#endif

/**
 * Concatenate two strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \return a concatenation of the two parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1
);

/**
 * Concatenate three strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \return a concatenation of the three parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2
);

/**
 * Concatenate four strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \return a concatenation of the four parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3
);

/**
 * Concatenate five strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \return a concatenation of the five parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4
);

/**
 * Concatenate six strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \return a concatenation of the six parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5
);

/**
 * Concatenate seven strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \return a concatenation of the seven parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6
);

/**
 * Concatenate eight strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \return a concatenation of the eight parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7
);

/**
 * Concatenate nine strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \return a concatenation of the nine parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8
);

/**
 * Concatenate ten strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \return a concatenation of the ten parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9
);

/**
 * Concatenate eleven strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \param part10 eleventh part of the concatenation
 * \return a concatenation of the eleven parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9,
  const std::wstring& part10
);

/**
 * Concatenate twelve strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \param part10 eleventh part of the concatenation
 * \param part11 twelfth part of the concatenation
 * \return a concatenation of the twelve parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9,
  const std::wstring& part10,
  const std::wstring& part11
);

/**
 * Concatenate thirteen strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \param part10 eleventh part of the concatenation
 * \param part11 twelfth part of the concatenation
 * \param part12 thirteenth part of the concatenation
 * \return a concatenation of the thirteen parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9,
  const std::wstring& part10,
  const std::wstring& part11,
  const std::wstring& part12
);

/**
 * Concatenate fourteen strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \param part10 eleventh part of the concatenation
 * \param part11 twelfth part of the concatenation
 * \param part12 thirteenth part of the concatenation
 * \param part13 fourteenth part of the concatenation
 * \return a concatenation of the fourteen parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9,
  const std::wstring& part10,
  const std::wstring& part11,
  const std::wstring& part12,
  const std::wstring& part13
);

/**
 * Concatenate fifteen strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \param part10 eleventh part of the concatenation
 * \param part11 twelfth part of the concatenation
 * \param part12 thirteenth part of the concatenation
 * \param part13 fourteenth part of the concatenation
 * \param part14 fifteenth part of the concatenation
 * \return a concatenation of the fifteen parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9,
  const std::wstring& part10,
  const std::wstring& part11,
  const std::wstring& part12,
  const std::wstring& part13,
  const std::wstring& part14
);

/**
 * Concatenate sixteen strings.
 *
 * \param part0 first part of the concatenation
 * \param part1 second part of the concatenation
 * \param part2 third part of the concatenation
 * \param part3 fourth part of the concatenation
 * \param part4 fifth part of the concatenation
 * \param part5 sixth part of the concatenation
 * \param part6 seventh part of the concatenation
 * \param part7 eighth part of the concatenation
 * \param part8 ninth part of the concatenation
 * \param part9 tenth part of the concatenation
 * \param part10 eleventh part of the concatenation
 * \param part11 twelfth part of the concatenation
 * \param part12 thirteenth part of the concatenation
 * \param part13 fourteenth part of the concatenation
 * \param part14 fifteenth part of the concatenation
 * \param part15 sixteenth part of the concatenation
 * \return a concatenation of the sixteen parts
 */
std::wstring Concat(
  const std::wstring& part0,
  const std::wstring& part1,
  const std::wstring& part2,
  const std::wstring& part3,
  const std::wstring& part4,
  const std::wstring& part5,
  const std::wstring& part6,
  const std::wstring& part7,
  const std::wstring& part8,
  const std::wstring& part9,
  const std::wstring& part10,
  const std::wstring& part11,
  const std::wstring& part12,
  const std::wstring& part13,
  const std::wstring& part14,
  const std::wstring& part15
);

/**
 * Check if all the elements satisfy the \p condition.
 *
 * \param condition returning a boolean to be checked for each element
 * \param container to be iterated through
 * \return `true` if all the elements of \p container satisfy the \p condition
 */
template<typename ContainerT, typename FunctorT>
bool All(
  FunctorT condition,
  const ContainerT& container
) {
  for (const auto& item : container ) {
    if (!condition(item)) {
      return false;
    }
  }
  return true;
}

/**
 * Check if any of the elements satisfy the \p condition.
 *
 * \param condition returning a boolean to be checked for each element
 * \param container to be iterated through
 * \return `true` if any of the elements of \p container satisfy the \p condition
 */
template<typename ContainerT, typename FunctorT>
bool Some(
  FunctorT condition,
  const ContainerT& container
) {
  for (const auto& item : container ) {
    if (condition(item)) {
      return true;
    }
  }
  return false;
}

/**
 * Check if all the elements in the range `[start, end)` satisfy the \p condition.
 *
 * \param condition returning a boolean to be checked for each element
 * \param container to be iterated through
 * \param start index in the \p container
 * \param end index in the \p container
 * \return \parblock
 * `true` if all the elements in \p container between \p start and \p end (excluded)
 * satisfy the \p condition
 * \endblock
 */
template<typename ContainerT, typename FunctorT>
bool AllRange(
  FunctorT condition,
  const ContainerT& container,
  size_t start,
  size_t end
) {
  for (size_t i = start; i < end; ++i) {
    if (!condition(container.at(i))) {
      return false;
    }
  }
  return true;
}

/**
 * Check if any of the elements in the range `[start, end)` satisfy the \p condition.
 *
 * \param condition returning a boolean to be checked for each element
 * \param container to be iterated through
 * \param start index in the \p container
 * \param end index in the \p container
 * \return \parblock
 * `true` if any of the elements in \p container between \p start and 
 * \p end (excluded) satisfy the \p condition
 * \endblock
 */
template<typename ContainerT, typename FunctorT>
bool SomeRange(
  FunctorT condition,
  const ContainerT& container,
  size_t start,
  size_t end
) {
  for (size_t i = start; i < end; ++i) {
    if (condition(container.at(i))) {
      return true;
    }
  }
  return false;
}

/**
 * Convert platform-independent the wide string to a UTF-8 string.
 *
 * \param text to be converted
 * \return UTF-8 encoded \p text
 */
std::string WstringToUtf8(const std::wstring& text);

/**
 * Convert platform-independent the UTF-8 encoded string to a wide string.
 *
 * \param utf8_text UTF-8 encoded text to be converted
 * \param utf8_text_size size of the text to be converted. If std::string::npos,
 * the \p utf8_text is assumed to be null-terminated and the size is determined
 * using `strlen`.
 * \return the wide-string representation
 */
std::wstring Utf8ToWstring(
  const char* utf8_text,
  size_t utf8_text_size = std::string::npos
);

/**
 * Convert platform-independent the UTF-8 encoded string to a wide string.
 *
 * \param utf8_text UTF-8 encoded text to be converted
 * \return wide string
 */
std::wstring Utf8ToWstring(const std::string& utf8_text);

}  // namespace common
}  // namespace aas_3_0
}  // namespace aas_core

/**@}*/

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // AAS_CORE_AAS_3_0_COMMON_GUARD_
